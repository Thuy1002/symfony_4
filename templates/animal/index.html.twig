{% extends 'base.html.twig' %}

{% block title %} AnnimalController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>
           <div style=" margin: 0 346px;" class="mb-3">
                 Xin chào {{ app.user.username }}, <a href="{{ path('app_login') }}">Logout</a>
           </div>
    <div class="example-wrapper">

        <div>
            <form method="get" class="d-inline-flex" action="">
                <input class="input-group-text" type="text" name="search">
                <button  class="btn-group">Search</button>
            </form>
        </div>
        <h2><a href="/add" class="btn btn-success " >Thêm Mới</a></h2>
        <table class="table">
            <thead>
            <tr>
                <th>id
                <th>
                <th>name
                <th>
                <th>color
                <th>
                <th>weight
                <th>
                <th><a href="">Action</a>
                <th>
            </tr>
            </thead>
            <tbody>
            {% for an in An %}
                <tr>
                    <th>{{ an.id }}
                    <th>
                    <th>{{ an.name }}
                    <th>
                    <th>{{ an.color }}
                    <th>
                    <th>{{ an.weight }} kg
                    <th>
                    <th>
                        <a href="/animal/edit/{{ an.id }}" class="btn btn-light">Edit</a>
                        <a href="/animal/delete/{{ an.id }}" class="btn btn-danger">Delete</a>

                    <th>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
